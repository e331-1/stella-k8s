apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pitpe-app-certificate
  namespace: kube-system
spec:
  secretName: pitpe-app-tls 
  dnsNames:
    - "*.pitpe.app"
    - "pitpe.app"
  issuerRef:
    kind: ClusterIssuer 
    name: ca-issuer